window.addEventListener("load", function() {
    // Cache le loader une fois que la page est complètement chargée
    const loaderWrapper = document.getElementById("loader-wrapper");
    loaderWrapper.style.display = "none";
});